[pytest]
# Explicitly add the current directory (project root, represented by '.') 
# to the Python search path. This resolves the ModuleNotFoundError 
# when importing packages like 'scripts' from tests/.
python_paths = .

# Set the pattern for test file discovery
python_files = test_*.py